# Core (routing + serving)
fastapi[standard]   # includes the `fastapi dev` CLI, uvicorn, httpx deps
httpx               # explicit: used directly in the gateway + test scripts

# Data store / client (rate limiting, caching)
redis

# Observability
prometheus-client

# Testing
pytest
pytest-asyncio      # lets async tests run (the concurrency/atomicity test)
